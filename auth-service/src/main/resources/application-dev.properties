# ============================================================================
# Development Profile - H2 Database Configuration
# ============================================================================

# ============================================================================
# Database Configuration - H2 In-Memory
# ============================================================================
spring.datasource.url=${TEMP_AUTH_SERVICE_DB_URL:jdbc:h2:mem:authdb}
spring.datasource.driverClassName=${TEMP_AUTH_SERVICE_DB_DRIVER:org.h2.Driver}
spring.datasource.username=${TEMP_AUTH_SERVICE_DB_USERNAME:sa}
spring.datasource.password=${TEMP_AUTH_SERVICE_DB_PASSWORD:}

# ============================================================================
# Connection Pool Configuration (Hikari)
# ============================================================================
# H2 uses default Hikari settings

# ============================================================================
# H2 Console Configuration
# ============================================================================
spring.h2.console.enabled=${TEMP_AUTH_SERVICE_H2_CONSOLE_ENABLED:true}
spring.h2.console.path=${TEMP_AUTH_SERVICE_H2_CONSOLE_PATH:/h2-console}

# ============================================================================
# JPA/Hibernate Configuration
# ============================================================================
spring.jpa.database-platform=${TEMP_AUTH_SERVICE_JPA_DIALECT:org.hibernate.dialect.H2Dialect}
spring.jpa.hibernate.ddl-auto=${TEMP_AUTH_SERVICE_JPA_DDL_AUTO:create-drop}
spring.jpa.show-sql=${TEMP_AUTH_SERVICE_JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${TEMP_AUTH_SERVICE_JPA_FORMAT_SQL:true}
spring.jpa.properties.hibernate.use_sql_comments=${TEMP_AUTH_SERVICE_JPA_SQL_COMMENTS:true}
spring.jpa.defer-datasource-initialization=${TEMP_AUTH_SERVICE_JPA_DEFER_INIT:true}
spring.sql.init.mode=${TEMP_AUTH_SERVICE_SQL_INIT_MODE:always}

# ============================================================================
# Logging Configuration
# ============================================================================
logging.level.root=INFO
logging.level.com.template.business=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ============================================================================
# JWT Configuration
# ============================================================================
# WARNING: This is for development ONLY - DO NOT use in production!
jwt.secret=${TEMP_AUTH_SERVICE_JWT_SECRET:9ff7ff40e4641ffc5e1078ce57f682723c7371612ecc84a0e9c60e786f90cfb0}

# Access Token (short-lived) - 15 minutes (900000 ms)
jwt.access.expiration=${TEMP_AUTH_SERVICE_JWT_ACCESS_EXPIRATION:900000}
# Backward compatibility
jwt.expiration=${TEMP_AUTH_SERVICE_JWT_ACCESS_EXPIRATION:900000}

# Refresh Token (long-lived) - 24 hours (86400000 ms)
jwt.refresh.expiration=${TEMP_AUTH_SERVICE_JWT_REFRESH_EXPIRATION:86400000}

# ============================================================================
# Session Management Configuration
# ============================================================================
# Maximum number of concurrent sessions per user
session.max-per-user=${TEMP_AUTH_SERVICE_SESSION_MAX_PER_USER:5}

# ============================================================================
# Mailing Scheduler Configuration
# ============================================================================
mailing.scheduler.enabled=false
spring.mail.host=${TEMP_AUTH_SERVICE_MAIL_HOST:localhost}
spring.mail.port=${TEMP_AUTH_SERVICE_MAIL_PORT:25}

# ============================================================================
# LDAP Configuration
# ============================================================================
ldap.enabled=${TEMP_AUTH_SERVICE_LDAP_ENABLED:false}
ldap.url=${TEMP_AUTH_SERVICE_LDAP_URL:ldaps://ldap.example.com:636}
ldap.base=${TEMP_AUTH_SERVICE_LDAP_BASE:example.local}
ldap.userSearchFilter=${TEMP_AUTH_SERVICE_LDAP_USER_SEARCH_FILTER:(&(objectClass=user)(sAMAccountName={1}))}
